apiVersion: v1
kind: Service
metadata:
  name: {{ include "gutendex.fullname" . }}
  labels:
    {{- include "gutendex.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "gutendex.selectorLabels" . | nindent 4 }}

